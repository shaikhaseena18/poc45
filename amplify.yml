version: 1

frontend:
  phases:
    preBuild:
      commands:
        - npm install -g sass
    build:
      commands:
        - sass styles.scss styles.css

  artifacts:
    baseDirectory: .
    files:
      - '**/*'
